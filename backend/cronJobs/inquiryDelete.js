{/*
    const cron = require("node-cron");
const { deleteOldResolvedInquiries } = require("../controllers/inquiriesService");

cron.schedule("0 0 * * *", async () => {
  console.log("Running job to delete old resolved inquiries...");
  await deleteOldResolvedInquiries();
});

*/}
